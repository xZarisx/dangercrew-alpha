import React from 'react'
import { connect } from 'react-redux'

@connect((state, props) => {
    return {
    }
})

class PauseItemsContent extends React.Component {
    /* TODO */
    render() {
        return (
            <div>ITEMS stuff goes here</div>
        );
    }
}

export default PauseItemsContent;